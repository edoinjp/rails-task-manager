<%= form_with model: @task do |t| %>
  <%= t.label :title, for: "title"%>
  <%= t.text_field :title %>
  <%= t.label :details, for: "details" %>
  <%= t.text_field :details %>
  <%= t.submit %>
<% end %>
